{#-
  Every deprecation the registry knows, as the analyzers see it.

  OpenTelemetryDeprecatedSemconvCatalog.cs is hand-curated — it carries since-versions,
  guidance prose, and mappings for keys upstream deleted outright, none of which the registry
  contains — so it cannot be generated. What can rot silently is the part the registry does
  know, and a Pipeline test cross-checks the catalog against this table: a renamed attribute
  must appear in the catalog with exactly the registry's replacement, an attribute deprecated
  without a rename must appear at all, and a deprecated enum member must appear under its
  attribute. This replaces verify_deprecated_catalog.py.

  Renames here are the registry's direct `renamed_to`, not the transitive resolution
  AttributeMapping does: the catalog states the one-step migration a consumer is told to make.
-#}
// <auto-generated/>
// Generated by Weaver {{ params.weaver_version }} from qyl registry {{ params.qyl_schema_url }} (core {{ params.core_ref }}, genai {{ params.genai_commit }})
// Regenerate with scripts/generate.sh; do not edit by hand.
// </auto-generated>

#nullable enable

namespace Qyl.Telemetry.SemanticConventions.Analyzers;

/// <summary>
/// The deprecations the pinned registry declares: renamed attributes with their one-step
/// replacement, attributes deprecated without one, and deprecated enum members.
/// </summary>
internal static class SemconvDeprecations
{
    /// <summary>Attribute key to its registry <c>renamed_to</c> replacement.</summary>
    internal static readonly (string Key, string RenamedTo)[] Renamed =
    {
{% for a in ctx.renamed %}
        ("{{ a.key }}", "{{ a.renamed_to }}"),
{% endfor %}
    };

    /// <summary>Attributes the registry deprecates without naming a replacement.</summary>
    internal static readonly string[] DeprecatedWithoutReplacement =
    {
{% for k in ctx.without_replacement %}
        "{{ k }}",
{% endfor %}
    };

    /// <summary>Deprecated enum members, as attribute key and member value.</summary>
    internal static readonly (string Key, string Value)[] DeprecatedValues =
    {
{% for m in ctx.deprecated_values %}
        ("{{ m.key }}", "{{ m.value }}"),
{% endfor %}
    };
}
