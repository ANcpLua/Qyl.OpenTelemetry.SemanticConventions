<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Qyl.OpenTelemetry.SemanticConventions.Build</RootNamespace>
    <AssemblyName>_build</AssemblyName>

    <IsPackable>false</IsPackable>
    <!-- CA1707: Nuke convention requires the assembly to be named `_build` for the
         entry-point discovery; the underscore is intentional. Other suppressions
         are standard for a Nuke build host (top-level program, no async ctx). -->
    <NoWarn>$(NoWarn);CA1050;CA1707;CA1822;CA2007;CS1591</NoWarn>

    <!-- Build host: don't enforce documentation-comment generation. -->
    <GenerateDocumentationFile>false</GenerateDocumentationFile>

    <NukeRootDirectory>../..</NukeRootDirectory>
    <NukeScriptDirectory>../..</NukeScriptDirectory>
  </PropertyGroup>

  <ItemGroup>
    <!-- Canonical Nuke build component from the O-ANcppLua org, shared across the
         qyl OpenTelemetry-conventions toolchain. Provides IUpstreamConventions,
         IDomainConventionsApi, and LockstepPolicy. -->
    <PackageReference Include="ANcpLua.OpenTelemetry.Conventions.Nuke"/>
  </ItemGroup>

  <!-- Transitive security overrides for Nuke.Common 10.1.0
       (GHSA-g4vj-cjjj-v7hg / GHSA-37gx-xxp4-5rgx). The canonical Nuke component pins
       these with PrivateAssets="all", which suppresses transitive flow into the build
       host, so we re-declare them here for the dotnet-run executable. -->
  <ItemGroup>
    <PackageReference Include="NuGet.Packaging"/>
    <PackageReference Include="System.Security.Cryptography.Xml"/>
  </ItemGroup>

</Project>
